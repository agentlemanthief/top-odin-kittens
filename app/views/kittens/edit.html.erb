<% if flash[:error] %>
  <%= flash[:error] %>
<% end %>

<h1>Edit Kitten</h1>

<%= render 'form' %>

<%= link_to 'Back', :back %>
<%= link_to "Delete", kitten_path(@kitten), method: :delete, data: { confirm: "Are you sure?" } %>
